# devkit/prj.conf
# Minimal Kconfig for STM32F411E-DISCO devkit bring-up.
# LOG via RTT (no USB-UART needed — reads through existing STLink debug USB).

# Logging
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=3
CONFIG_LOG_PROCESS_THREAD_STACK_SIZE=1536
CONFIG_LOG_BACKEND_UART=n
CONFIG_LOG_BACKEND_RTT=y

# RTT (Segger J-Link RTT — supported by openocd xpack)
CONFIG_USE_SEGGER_RTT=y
CONFIG_SEGGER_RTT_BUFFER_SIZE_UP=4096

# GPIO (for LED blink)
CONFIG_GPIO=y

# Shell + UART disabled (ST-LINK/V2 acts as SWD debug only, no VCP)
CONFIG_SHELL=n
CONFIG_SERIAL=n
CONFIG_UART_CONSOLE=n

# Disable CTF tracing
CONFIG_TRACING=n
